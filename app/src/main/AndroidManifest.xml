<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet permission is required for API calls (Retrofit) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Access network state is required when checking connectivity status -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Storage permission is required for Room to cache data on device. If denied, fallback to in-memory cache (see repository). -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Promotr">
        <!-- SplashActivity is the entry point for splash, permissions, onboarding, guest mode, etc. -->
        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- MainActivity removed per request; onboarding will be the next step after splash. -->
        <activity
            android:name=".ui.onboarding.OnboardingActivity"
            android:exported="true" />
        <!-- MainActivity may be a top-level stub used for navigation after onboarding -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>